
export default function AddQuantity(props){


return (
   <div>
{/*             <label htmlFor="product-quantity">
              {' '}
            quantity
              </label>
              <input
                name="selectQuantity"
                data-test-id="product-quantity"
                type="number"
                min="1"
                max="10"
                value={props.quantity}
                onChange={(event) => addToCartEventHandler(
                  props.plant.id,
                  props.quantity,
                  // props.cartCookie,
                  props.cartCookie,
                )}
              /> */}
          </div>
);

}