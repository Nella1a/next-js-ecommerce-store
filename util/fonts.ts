import { Calistoga, Roboto } from 'next/font/google';

export const calistoga = Calistoga({ weight: '400', subsets: ['latin'] });
export const roboto = Roboto({
  weight: ['400', '500', '700', '900'],
  subsets: ['latin'],
});
